# Use only the wrapper's empty credential file, never external helpers.
# Images are fully qualified; no registry search, remapping, or mirror is needed.
credential-helpers = ["containers-auth.json"]
